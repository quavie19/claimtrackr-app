GET /api/admin/dashboard           // Admin dashboard with stats on claims, users, etc.
PUT /api/admin/claims/:claimId      // Update claim details (admin only)
DELETE /api/admin/claims/:claimId   // Delete a claim (admin only)
GET /api/admin/users               // Get all users (admin only)
POST /api/admin/users              // Create a new user (admin only)
