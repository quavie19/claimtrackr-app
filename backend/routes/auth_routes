const express = require('express');
const router = express.Router(); // Use Router instead of express()
const { activate } = require('../controllers/auth_controller'); // Destructure signup from auth_controller

// Define the route
router.post('/activate', activate); // Ensure signup is a function

// Export the router
module.exports = router; // Export the router directly
