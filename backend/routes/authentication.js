POST / api / auth / register; // Register a new user
POST / api / auth / login; // Login a user
POST / api / auth / logout; // Logout the user
GET / api / auth / me; // Get the current logged-in user's profile (protected)
