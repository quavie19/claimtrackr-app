GET /api/claims/:claimId/status         // Get the current status of a claim
POST /api/claims/:claimId/status        // Create a status update (admin or adjuster)
PUT /api/claims/:claimId/status/:statusId  // Update a specific status (admin or adjuster)
