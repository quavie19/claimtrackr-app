POST /api/claims/:claimId/assignments      // Assign a contractor or adjuster to a claim (admin)
GET /api/claims/:claimId/assignments       // Get all assignments for a claim
DELETE /api/claims/:claimId/assignments/:assignmentId  // Remove a contractor or adjuster from a claim (admin)
